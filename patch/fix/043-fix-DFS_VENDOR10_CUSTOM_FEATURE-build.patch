--- a/mt_wifi/embedded/common/cmm_profile.c
+++ b/mt_wifi/embedded/common/cmm_profile.c
@@ -540,7 +540,6 @@ static VOID RTMPOldChannelCfg(RTMP_ADAPTER
 static VOID RTMPOldChannelCfg(RTMP_ADAPTER *pAd, RTMP_STRING *Buffer)
 {
 	UINT32 i;
-	CHAR *macptr;
 	struct wifi_dev *wdev;
 	UCHAR BackupChannel;
 
@@ -553,7 +552,7 @@ static VOID RTMPOldChannelCfg(RTMP_ADAPTER
 	IF_DEV_CONFIG_OPMODE_ON_AP(pAd) {
 		/* Extract Channel */
 		BackupChannel = os_str_tol(Buffer, 0, 10);
-		for (i = 0; i < pAd->ApCfg.BssidNum; i++)
+		for (i = 0; i < pAd->ApCfg.BssidNum; i++) {
 			/* V10 -- Only 5G Mode */
 			wdev = &pAd->ApCfg.MBSSID[i].wdev;
 			if ((wdev == NULL) || (!WMODE_CAP_5G(wdev->PhyMode)))
--- a/mt_wifi/embedded/ap/ap.c
+++ b/mt_wifi/embedded/ap/ap.c
@@ -413,10 +413,11 @@ UCHAR ApAutoChannelAtBootUp(RTMP_
 			MTWF_DBG(pAd, DBG_CAT_ALL, DBG_SUBCAT_ALL, DBG_LVL_INFO, "[ApAutoChannelAtBootUp] ACS of BandIdx = %d is already DONE, Channel = %d\n", BandIdx, NewChannel);
 		}
 #if defined(DFS_MT7916_DEDICATED_ZW) || defined(DFS_MT7981_DEDICATED_ZW)
-		if (pAd->CommonCfg.DfsParameter.BW160DedicatedSup == TRUE)
+		if (pAd->CommonCfg.DfsParameter.BW160DedicatedSup == TRUE) {
 			DfsZeroWaitBW160StateUpdate(pAd, &vht_bw, NewChannel);
 
 			wlan_config_set_vht_bw(wdev, vht_bw);
+		}
 #endif
 #ifdef DFS_VENDOR10_CUSTOM_FEATURE
 		/* Update channel of wdev as new channel */
--- a/mt_wifi/embedded/include/wapp/wapp_cmm_type.h
+++ b/mt_wifi/embedded/include/wapp/wapp_cmm_type.h
@@ -953,6 +953,7 @@ typedef union GNU_PACKED _wapp_event_data {
 	wdev_vht_cap vht_cap;
 	wdev_misc_cap misc_cap;
 	wapp_client_info cli_info;
+	wdev_chn_info chn_list;
 	wdev_op_class_info op_class;
 	wdev_bss_info bss_info;
 	wdev_ap_metric ap_metrics;
--- a/mt_wifi/embedded/common/he_cfg.c
+++ b/mt_wifi/embedded/common/he_cfg.c
@@ -97,7 +97,7 @@ VOID  UniEventDisplayDbgStatus(struct
 VOID  UniEventDisplayDbgStatus(struct TXCMD_DBG_CMD_STATUS *status)
 {
 	MTWF_PRINT("debug mode: %s\n", (status->enable) ?
-			 "enable" : "disable"));
+			 "enable" : "disable");
 	MTWF_PRINT("TXCMD TX statistics:\n"
 			 "total=%d, protect=%d, txdata=%d, trigdata=%d, sw_fid=%d\n",
 			 status->txcmd_tx_count, status->txcmd_protect_count,
